
trigger:
- '*'

pool:
  vmImage: 'ubuntu-latest'  # Specify the agent pool and VM image

stages:
- stage: MyStage
  jobs:
  - template: RunMyJob.yml
    parameters:
      param1: 'value1'
      param2: 'value2'
      jobname: 'sachin'
      india: 'canada'
  
  - template: test.yml 
    parameters:
      india: 'canada'
