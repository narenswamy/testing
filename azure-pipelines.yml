trigger:
- '*'

pool:
  vmImage: 'ubuntu-latest'  # Specify the agent pool and VM image

stages:
- stage: MyStage
  jobs:
  - job: 
    steps:
    - template: RunMyJob.yml  # First template
       parameters:
         param1: 'value1'
         param2: 'value2'
         jobname: 'sachin'

    
  - job: 
    dependsOn:
    steps:
    - template: RunMyJob.yml  # First template
       parameters:
         param1: 'value1'
         param2: 'value2'
         jobname: 'sachin'
